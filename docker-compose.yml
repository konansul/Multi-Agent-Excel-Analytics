services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: excel_analytics
      POSTGRES_USER: excel
      POSTGRES_PASSWORD: excel
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: