import streamlit as st

def render_tab_visualization():
    st.subheader("Visualization (after signals)")
    st.info("Здесь будет: plot plan (agent) → generate plots (matplotlib) → показать графики.")
    st.write("Пока это placeholder.")